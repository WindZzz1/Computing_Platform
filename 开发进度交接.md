# 开发进度交接

---

## 2026-06-21 补充（模块 D 报表补全 + P1 修复）

> 本日新增 8 个 PR（模块 D 4 个 + P1 4 个），全部 assignee=WindZzz1、`Closes #Issue`，**待验收合并**（HEAD 仍 `d5dac2c`）。

### 模块 D 报表补全（需求 4.D，此前全是空壳）
| PR | Issue | 内容 |
|---|---|---|
| #126 | #125 | D-2 雷达图 + 抽取 `MajorScopeHelper`（专业/学年/年级 scope 复用） |
| #128 | #127 | D-2 穿透台账五层组装 + `AssessmentPointAccount` 对齐前端 + 废弃坏 `PenetrationAccountMapper` |
| #130 | #129 | D-2 台账 Excel 导出（5 sheet） |
| #132 | #131 | D-1 课程报表 PDF（嵌入 LXGW WenKai CJK TTF + `PdfTableRenderer` + pom filtering 修复） |

合并顺序：#126 → #128 → #130（串行；#128/#130 base 暂指向依赖分支，#126 合并后 retarget main）；#132 独立。E2E 已过（admin 临时放行测雷达图/台账真实数据 + PDF 30KB `%PDF-1.4`）。

### P1 修复（T3）
| PR | Issue | 内容 |
|---|---|---|
| #134 | #133 | 前端响应拦截器（40100 → 清登录态 + 跳 /login） |
| #136 | #135 | `CourseServiceImpl.importCourses` 原子性（去逐行 catch，任一行失败回滚） |
| #138 | #137 | HikariCP keepalive（max-lifetime/keepalive-time/connection-test-query） |
| #140 | #139 | 死代码清理（删 mock.ts/dict.ts + types 旧类型 + `GradeEntryServiceImpl.getCurrentUserId`） |

### 决策记录
- `validateMajorPermission` 对 leader 恒 true：`SysUser` 无 `major_id`（只有 collegeId），专业级收紧需 schema 变更，**决策保持现状、标注遗留**（PR-2/3 已注释）。
- PDF 字体：PDFBox 2.0.x 不支持 CFF-OTF/变量 TTF → 用 LXGW WenKai 静态 TTF（OFL，~24MB 入库，subset 后 PDF 不膨胀）。
- gh 认证：fine-grained PAT 不能 assign issue/PR → 改用 classic PAT（repo scope）+ `GH_TOKEN` 前缀 assign。gh 在 `C:\Program Files\GitHub CLI\gh.exe`。

### 待办（需人工）
1. classic PAT（`ghp_NMEh1...`）用完撤销：https://github.com/settings/tokens
2. 按顺序合并 8 个 PR：#126→#128→#130→#132（模块 D）+ #134/#136/#138/#140（P1 独立）
3. leader/edu 账号做前端 UI 联调（admin 被雷达图/台账 403）

---

## 2026-06-20（历史，原内容）

# 开发进度交接（2026-06-20）

> 本文档记录今天已完成的修复、当前系统状态、明日待办与关键代码位置，方便次日继续开发。
> 仓库：`WindZzz1/Computing_Platform`，所有今日 PR 已合并入 `main`（HEAD `d5dac2c`，编译通过）。

---

## 一、今日完成（全部已合并到 main）

### T1 致命问题（5 个）
| PR | 类别 | 解决了什么 |
|---|---|---|
| #110 | T1-3 鉴权 | `AuthInterceptor` 真正校验 `anyRole`（原先只读 `mustRole`，多角色接口对任意登录用户开放） |
| #111 | T1-1 事务 | `calculateAchievement`/`calculateMajorAchievement` 计算失败时 `setRollbackOnly()` 显式回滚，避免"先删后插"中途失败丢数据 |
| #112 | T1-2 范围 | `MajorCalculationServiceImpl.getTeachingClasses` 按 `course.major_id`+`grade` 过滤（原先空实现→跨专业串数据） |
| #113 | T1-4 锁定 | 新增 `GradesheetStatusHelper` 推断三态（未提交/已提交未算/已锁定）+ 防重算（`forceRecalculate`）+ 看板 status 字段 |
| #114 | D-1 报表 | `CourseAchievementReportServiceImpl` 实现数据接口 + Excel 导出；顺带修报表接口未纳入 JWT 拦截器白名单（一直返回"未登录"） |

### T2 严重问题（5 个）+ 1 个 hotfix
| Issue | PR | 类别 | 解决了什么 |
|---|---|---|---|
| — | #115 | hotfix | 补回 #112/#113 合并冲突丢失的 `MajorCalculationServiceImpl.studentMapper` 注入（曾导致 main 编译失败） |
| — | #116 | T2-6 公式 | 一级达成度公式改回规约 `Σ得分/Σ满分`（原为加权平均，结果不同） |
| — | #117 | T2-3 越权 | 新增 `OwnershipHelper`，11 处写操作加数据级归属校验（班级级 `teacher_id`、课程级"是否讲授该课程"），admin 放行 |
| #118 | #119 | T2-1 角色 | 宏观矩阵配置接口 `ROLE_EDU`→`ROLE_LEADER`（规约 A-4 要求专业负责人） |
| #120 | #121 | T2-5 导入 | 三个 Excel 批量导入改原子（任一行失败整批回滚，原先部分成功落库） |
| #122 | #123 | T2-4 唯一键 | `student_score` 唯一键加 `class_id`：`uk_student_point` → `uk_class_student_point(teaching_class_id, student_id, point_id)` |

> 每个 PR 均含：编译通过 + 单测（#110=9、#112=5、#113=4、#116=6、#117=8）或 E2E 验证 + 详细正文。
> T2-2（考核点绑唯一课程目标）**按你的决定保持多对多，已跳过**。

---

## 二、当前系统状态

- **main 健康**：HEAD `d5dac2c`，`mvn compile` 通过；#116+#117 都改过 `AchievementCalculationServiceImpl`（不同方法），合并无冲突。
- **远程生产库 schema 已变更**（T2-4）：`student_score` 唯一键已是 `uk_class_student_point`，旧 `uk_student_point` 已替换（已 `SHOW INDEX` 验证）。
- **GitHub Issues #118/#120/#122 已自动关闭**（对应 PR 用 `Closes #N`）。

### 运行方式
```bash
# 后端（端口 8101，context-path /api）
cd backend/Computing_Platform_backend/springboot-init-master
mvn spring-boot:run

# 前端（端口 5173）
cd frontend/obe-frontend
npm run dev
```
- 测试账号：`admin / 12345678`（远程库；README 里的默认密码 123456 不对）
- 远程 MySQL：`120.27.221.219` / 库 `graduation_achievement`（配置在 `backend/.../src/main/resources/application.yml`）
- 环境：JDK 21（target 17）、Maven 3.9、Node 24；本机 `mysql` 客户端可用（`C:\Program Files\MySQL\MySQL Server 8.0\bin`）

---

## 三、明日待办（按优先级）

### P0 — 功能补全（模块 D 剩余，目前是空壳 stub）
1. **D-1 PDF 导出**（`CourseAchievementReportServiceImpl.exportPdfReport`，现 throw "暂未实现"）
   - 硬约束：PDFBox 内置字体不支持中文，必须嵌入 CJK 字体。
   - **待决策**：① 往 `resources/fonts/` 加开源 CJK 字体（如思源/Noto 子集，几 MB，最稳）；② 用运行环境系统字体（Win 开发机有，部署 Linux 需自装）。建议 ①。
   - 数据已就绪：`generateReportData()` 已返回完整 VO，PDF 直接渲染即可。
2. **D-2 专业雷达图 + 穿透台账**（`MajorReportServiceImpl` 三个方法全 throw "待完整实现"）
   - 雷达图：`getRadarChartData` 只需返回数据（前端画图），数据源 `major_indicator_achievement` 已有。
   - 穿透台账：**先修 `PenetrationAccountMapper` 的 SQL 致命错误**：
     - `tc.major_id` 列不存在（`teaching_class` 无 major_id）→ 改走 `course.major_id` 关联
     - `student_score.class_id` 列名错 → 应为 `teaching_class_id`
     - 4 条 SQL 都没按 `termId/grade` 过滤
     - 目前是 4 张平铺表，**无 `major_indicator→course_indicator→student_objective→student_score` 的真 JOIN**，穿透语义不成立
   - D-2 权限：`@AuthCheck(anyRole=...)` 已在 #110 修复生效；`MajorReportServiceImpl.validateMajorPermission` 对 leader 恒 true（不校验专业归属），可后续收紧。

### P1 — 一般问题（T3，非阻塞）
1. **前端无响应拦截器**：`frontend/obe-frontend/src/api/request.ts` 只有请求拦截器；后端返回 `code===40100`（未登录）时不会自动 `logout()`+跳 `/login`，只弹 error。建议加响应拦截器。
2. **`CourseServiceImpl.importCourses`（JSON 列表导入，非 Excel）**：有与 T2-5 相同的"@Transactional 内逐行 catch 吞异常"原子性 bug，本轮只修了 Excel 的三个，这个待修。
3. **HikariCP 连接池 stale**（运维）：后端长时间运行后远程 MySQL 关闭空闲连接，HikariCP 不恢复→登录报 `code 50000`，重启可临时恢复。建议 `application.yml` 加：
   ```yaml
   spring.datasource.hikari:
     max-lifetime: 600000
     keepalive-time: 120000
     connection-test-query: SELECT 1
   ```
4. **死代码清理**（建议）：`src/api/mock.ts`、`src/api/dict.ts`、`src/types/index.ts` 旧类型、`GradeEntryServiceImpl.getCurrentUserId`（死方法）。

### 记录（已决策不动）
- **T2-2 考核点绑课程目标**：保持多对多（`rel_point_objective` + DTO 的 `objectiveIds`），不强制唯一。
- **teaching_class 不加 major_id**：专业关联走 `course.major_id`（#112 已用此方案）。

---

## 四、关键代码位置（明日快速上手）

| 关注点 | 位置 |
|---|---|
| 三级达成度计算 | `service/impl/AchievementCalculationServiceImpl`（一级目标级、二级课程级）、`MajorCalculationServiceImpl`（三级专业级） |
| 一级公式（规约 Σ得分/Σ满分） | `AchievementCalculationServiceImpl.computeObjectiveAchievement`（包级静态，已单测） |
| 成绩单锁定状态推断 | `manager/GradesheetStatusHelper`（三态：NOT_SUBMITTED/SUBMITTED/LOCKED） |
| 数据级归属校验 | `manager/OwnershipHelper`（`checkClassOwnership`/`checkCourseOwnership`，admin 放行） |
| 鉴权 AOP | `aop/AuthInterceptor`（读 mustRole + anyRole）、`aop/SysJwtInterceptor`（设 currentUser）、`config/WebMvcConfig`（拦截器白名单） |
| 宏观矩阵 ∑W=1.0 校验 | `MatrixCourseIndicatorServiceImpl.checkMatrixWeights`（后端强制，容差 0.0001） |
| 内部权重 ∑w=1.0 校验 | `WeightObjectiveIndicatorServiceImpl.checkWeights` |
| 报表（D-1 已实现 / D-2 待实现） | `service/impl/CourseAchievementReportServiceImpl`、`service/impl/MajorReportServiceImpl` |
| 数据库 schema | `sql/create_table.sql`（student_score 唯一键已更新） |

### 计算传导链（自下而上）
```
考核点得分(student_score)
  → 一级：学生课程目标达成度 C_ij = Σ实际得分 / Σ目标满分     [教师触发]
  → 二级：课程指标点达成度 E_k = Σ(C_j × w_jk)，∑w=1.0       [教师触发]
  → 三级：专业指标点达成度 G_k = Σ(E_c × W_c)，∑W=1.0        [专业负责人/教务全局触发]
```

---

## 五、开发工作流约定（请沿用）

1. **每个 PR 先建 Issue**（`assignees: WindZzz1`，`labels: bug/feat`），描述问题+方案。
2. **PR 正文首行写 `Closes #<Issue号>`**（合并后自动关 Issue），`assignees: WindZzz1`。
3. **PR 拆分**：一类问题一个 PR，从 `main` 建独立分支（`fix/...` 或 `feat/...`）。
4. **合并方式**：merge-commit（你一直用的方式）。
5. ⚠️ **合并教训**：多个 PR 同改某文件的 `@Resource` 注入块会撞冲突、可能吞字段（#115 hotfix 就是 #112/#113 合并丢了 `studentMapper`）。**同文件注入尽量错开位置，合并前本地编译自测**。
6. **验证标准**：编译 + 单测（Mockito/JUnit5，注意 surefire 已移除 `-XX:MaxPermSize`）+ 关键项 E2E（admin 账号 + curl）。

---

## 六、明日建议起点

1. 先 `git checkout main && git pull`（已是 `d5dac2c`，编译通过）。
2. 后端 `mvn spring-boot:run`、前端 `npm run dev` 起服务，用 `admin/12345678` 登录验证基线。
3. 按优先级开 **D-1 PDF**（先定 CJK 字体方案）或 **D-2 穿透台账**（先修 PenetrationAccountMapper 的 SQL）。
