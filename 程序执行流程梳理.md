# 程序执行流程梳理

> 面向专业认证的毕业要求达成度统一计算平台 —— 从初始数据录入到三级达成度计算完成的完整执行流程。
>
> 本文**以代码为准**（已对照 `sql/create_table.sql` 与各 ServiceImpl 核实）。最后更新：2026-06-27。

---

## 目录

1. [角色与权限模型（RBAC）](#一角色与权限模型rbac)
2. [数据模型与表关系](#二数据模型与表关系)
3. [整体计算传导链](#三整体计算传导链)
4. [分阶段详细流程](#四分阶段详细流程)
5. [三级达成度公式详解](#五三级达成度公式详解)
6. [权重校验规则（∑W=1.0 / ∑w=1.0）](#六权重校验规则w10--w10)
7. [成绩单三态状态机](#七成绩单三态状态机)
8. [关键约束与注意事项](#八关键约束与注意事项)
9. [附录：表清单与关键代码位置](#九附录表清单与关键代码位置)

---

## 一、角色与权限模型（RBAC）

系统采用 **三层防护**：

| 层级 | 实现 | 作用 |
|------|------|------|
| ① JWT 认证 | `SysJwtInterceptor` | 从 `Authorization: Bearer <token>` 解析用户，写入 `request.currentUser`；校验账号是否禁用 |
| ② 角色鉴权 | `@AuthCheck` 注解 + `AuthInterceptor` (AOP) | 按 `mustRole`（精确匹配）/ `anyRole`（任一匹配）校验功能权限 |
| ③ 数据归属 | `OwnershipHelper` | 防横向越权，确保教师只能操作自己主讲的班/课 |

> `admin` 角色在 ②③ 两层均直接放行（超级管理员）。

### 四大角色职责

| 角色码 | 角色 | 核心职责 | 数据归属校验 |
|--------|------|----------|------------|
| `admin` | 管理员 | 字典数据（学院/专业/学年）、账号、清理三级结果 | 全部放行 |
| `edu` | 教务管理员 | 用户/课程/教学班/学生管理 + 专业级计算与报表 | — |
| `leader` | 专业负责人 | 毕业要求/指标点/宏观矩阵 W + 专业级计算与报表 | — |
| `teacher` | 主讲教师 | 课程目标/考核点/内部权重 w/成绩录入/课程级计算与报表 | 仅自己主讲的班/课 |

角色常量定义：`SysUserConstant.java`（`ROLE_ADMIN / ROLE_EDU / ROLE_LEADER / ROLE_TEACHER`）。

### 角色 → 端点映射（已核实 @AuthCheck）

| Controller | 主要操作 | 鉴权 |
|------------|----------|------|
| `SysDictCollege/Major/SchoolYear` | 字典增删改查 | `admin` |
| `SysUser` | 按角色查用户 | `edu` |
| `Course` | 课程增删改查、Excel 导入 | `edu` |
| `Student` | 学生增删改查、Excel 导入 | `edu` |
| `TeachingClass` | 教学班增删改查、绑/解学生、Excel 导入 | `edu` |
| `GraduationRequirement` / `IndicatorPoint` | 毕业要求、指标点维护 | `leader` |
| `MatrixCourseIndicator` | 宏观支撑矩阵 W 配置/校验 | `leader` |
| `CourseObjective` / `AssessmentPoint` | 课程目标、考核点维护（含归属校验） | `teacher` |
| `WeightObjectiveIndicator` | 内部权重 w 配置/校验 | `teacher` |
| `GradeEntry` | 成绩模板/导入/查询/修改/清空 | `teacher` |
| `AchievementCalculation` | 触发课程级计算（一级+二级） | `teacher`（status/detail 为 `teacher,admin`） |
| `CourseAchievementReport` | 课程达成度报表（D-1） | `teacher` |
| `MajorCalculation` | 专业级计算（三级）/ 看板 / 结果 | `leader,edu`（删除为 `admin`） |
| `MajorReport` | 专业报表：雷达图/穿透台账（D-2） | `leader,edu` |

拦截路径与白名单见 `config/WebMvcConfig`：所有业务路径拦截，仅 `/sysuser/login`、`/sysuser/login/token` 免登录。

---

## 二、数据模型与表关系

> 全部表结构以 `backend/.../sql/create_table.sql` 为准。

### 实体依赖关系（外键箭头）

```
学院 sys_dict_college
  └─ 专业 sys_dict_major.college_id
        ├─ 课程 course.major_id
        │     ├─ 教学班 teaching_class.course_id (+teacher_id, +term_id)
        │     │     └─ 班级学生 class_student (teaching_class_id, student_id)
        │     ├─ 课程目标 course_objective.course_id
        │     ├─ 考核点 assessment_point.course_id
        │     │     └─ 考核点-目标 rel_point_objective (point_id, objective_id)  ← 多对多
        │     └─ 内部权重 weight_objective_indicator (course_id, objective_id, indicator_id)
        ├─ 学生 student.major_id (+grade)
        ├─ 毕业要求 graduation_requirement.major_id
        │     └─ 指标点 indicator_point.requirement_id
        └─ 宏观矩阵 matrix_course_indicator (major_id, course_id, indicator_id)
```

### 关键确认点

- **`teaching_class` 无 `major_id` 字段** —— 专业归属一律走 `course.major_id`（三级计算据此过滤，#112 修复）。
- **`student_score` 唯一键**：`uk_class_student_point (teaching_class_id, student_id, point_id)`（#123 修复，原为不含 class_id 的旧键）。
- **权重精度**：`matrix_course_indicator.total_weight` 与 `weight_objective_indicator.inner_weight` 均为 `DECIMAL(5,4)`。
- **三个达成度结果表**：`student_objective_achievement`（一级）、`course_indicator_achievement`（二级）、`major_indicator_achievement`（三级，按 `major_id+term_id+grade` 聚合）。

---

## 三、整体计算传导链

系统的核心是**自下而上、逐级聚合**的三级计算，每一级的输入是上一级的输出：

```
【录入】                【一级·teacher 触发】          【二级·teacher 触发】        【三级·leader/edu 触发】
考核点原始成绩     ──→   学生课程目标达成度 C_ij  ──→  课程指标点达成度 E_k   ──→  专业指标点达成度 G_k
student_score           (按课程目标聚合)              (班级均值 × w)             (课程均值 × W)
actual_score/full_score      ↓                            ↓                          ↓
                     student_objective_          course_indicator_         major_indicator_
                     achievement                 achievement               achievement
```

| 级别 | 名称 | 产出表 | 触发角色 |
|------|------|--------|----------|
| 一级 | 学生课程目标达成度 C_ij | `student_objective_achievement` | teacher |
| 二级 | 课程指标点达成度 E_k | `course_indicator_achievement` | teacher（与一级同一调用） |
| 三级 | 专业指标点达成度 G_k | `major_indicator_achievement` | leader / edu |

---

## 四、分阶段详细流程

### 阶段 0 ｜ 系统初始化（`admin`）

| # | 操作 | 涉及表 | 接口 | 前置 |
|---|------|--------|------|------|
| 0.1 | 录入学院 | `sys_dict_college` | `/dict/college/add` | 无 |
| 0.2 | 录入专业（挂学院） | `sys_dict_major` | `/dict/major/add` | 学院 |
| 0.3 | 录入学年学期 | `sys_dict_school_year` | `/dict/schoolyear/add` | 无 |
| 0.4 | 创建各角色账号（挂学院） | `sys_user` | `/sysuser/add` | 学院 |

> 字典数据是所有业务数据的根，**必须最先建立**。

### 阶段 1 ｜ 基础数据（`edu` 教务管理员）

| # | 操作 | 涉及表 | 接口 | Excel 导入 | 前置 |
|---|------|--------|------|-----------|------|
| 1.1 | 课程库（挂专业） | `course` | `/course/add`、`/course/import/excel` | ✅ | 专业 |
| 1.2 | 学生（挂专业） | `student` | `/student/import/excel` | ✅ | 专业 |
| 1.3 | 创建教学班（挂课程+教师+学年） | `teaching_class` | `/teaching-class/add` | ❌ | 课程、teacher 账号、学年 |
| 1.4 | 教学班绑定学生 | `class_student` | `/teaching-class/import-students/excel` | ✅ | 教学班、学生 |

### 阶段 2 ｜ 毕业要求与指标点（`leader` 专业负责人）

| # | 操作 | 涉及表 | 接口 | 前置 |
|---|------|--------|------|------|
| 2.1 | 配置毕业要求（挂专业） | `graduation_requirement` | `/requirement/graduation/add` | 专业 |
| 2.2 | 配置指标点（挂毕业要求） | `indicator_point` | `/requirement/indicator/add` | 毕业要求 |

> 指标点是三级计算的**最小目标单元**，所有权重与达成度都围绕它展开。

### 阶段 3 ｜ 课程大纲与权重配置（`teacher` + `leader` 协作）

**A. 课程层面（`teacher`）—— 配置「考核如何支撑目标」**

| # | 操作 | 涉及表 | 接口 | 说明 |
|---|------|--------|------|------|
| 3.1 | 配置课程目标 CO | `course_objective` | `/objective/add` | 归属校验：须为该课主讲教师 |
| 3.2 | 配置考核点（含满分 `full_score`） | `assessment_point` | `/assessment-point/add` | 一级公式分母来源 |
| 3.3 | 考核点 ↔ 课程目标关联（多对多） | `rel_point_objective` | `/assessment-point/bind-objective` | 决定一级聚合范围；关联权重默认 1.0 |
| 3.4 | 配置**内部权重 w**（目标→指标点） | `weight_objective_indicator` | `/weight/save`、`/weight/check` | ∑w=1.0 强制校验 |

**B. 专业层面（`leader`）—— 配置「课程如何支撑指标点」**

| # | 操作 | 涉及表 | 接口 | 说明 |
|---|------|--------|------|------|
| 3.5 | 配置**宏观支撑矩阵 W**（课程→指标点） | `matrix_course_indicator` | `/matrix/config`、`/matrix/check` | ∑W=1.0 强制校验 |

### 阶段 4 ｜ 成绩录入（`teacher`）

| # | 操作 | 接口 | 说明 |
|---|------|------|------|
| 4.1 | 下载成绩模板 | `/grade-entry/template` | 自动生成「学号/姓名 + 各考核点列（含满分）」 |
| 4.2 | Excel 导入成绩 | `/grade-entry/import` | 写入 `student_score`，**原子**（先物理删旧再批量插） |
| 4.3 | 修改 / 清空成绩 | `/grade-entry/update`、`/delete` | 均经 `checkClassOwnership` 归属校验 |

导入匹配规则：按**学号**匹配学生（校验姓名一致性），按**列序**匹配考核点；校验「分数 ≤ 满分、≥ 0」。

### 阶段 5 ｜ 一级 + 二级计算（`teacher` 触发，课程级达成度）

一次调用 `/achievement-calculation/calculate`（`mustRole=teacher`）**同时完成一级与二级**。

**前置校验**（`AchievementCalculationServiceImpl`）：
1. 归属校验 `checkClassOwnership`（仅本班主讲教师 / admin 放行）；
2. 防重算：LOCKED 状态需显式传 `forceRecalculate=true`；
3. 数据完整性：必须有学生、成绩、课程目标、考核点-目标关联、内部权重 w。

**计算过程**（`@Transactional` + 显式 `setRollbackOnly()`，先删后写）：
- **一级** `computeObjectiveAchievement` → `student_objective_achievement`
- **二级** → `course_indicator_achievement`
- 状态变更：`SUBMITTED → LOCKED`

### 阶段 6 ｜ 三级计算（`leader` / `edu` 触发，专业级达成度）

调用 `/major-calculation/calculate`（`anyRole=leader,edu`）。

**前置校验**：
1. `MajorScopeHelper.getTeachingClasses` 按 `course.major_id + term + grade` 过滤教学班（防跨专业串数据，#112）；
2. **所有支撑课程必须已完成二级计算**，否则报「课程 XXX 尚未计算达成度」；
3. 宏观矩阵 W 已配置。

**计算过程**（`@Transactional` + 显式回滚，先删后写）→ `major_indicator_achievement`。
三级结果**仅 `admin` 可删**（`/major-calculation/delete`）。

### 阶段 7 ｜ 报表导出

| 报表 | 角色 | 接口 | 内容 |
|------|------|------|------|
| 课程达成度报表（D-1） | teacher | `/course-achievement-report/{data,export/excel,export/pdf}` | 课程级，PDF 内嵌中文字体（LXGW WenKai） |
| 专业报表（D-2） | leader/edu | `/major-report/{radar-data,penetration-account,export/*}` | 雷达图、穿透台账 |

---

## 五、三级达成度公式详解

> 公式均以 `AchievementCalculationServiceImpl` / `MajorCalculationServiceImpl` 实际代码为准。

### 一级：学生课程目标达成度 C_ij

```
C_ij = Σ(支撑目标 j 的考核点实际得分) / Σ(支撑目标 j 的考核点满分)
```

- **分子**：`student_score.actual_score`（该学生在支撑目标 j 的所有考核点的实际得分）；
- **分母**：`assessment_point.full_score`（这些考核点的满分）；
- **聚合维度**：同一学生、同一课程目标下，经 `rel_point_objective` 关联的所有考核点；
- **缺考处理**：无成绩时分子不计入（等价 0），分母仍计入满分 → **缺考拉低达成度**；
- **关键点**：一级计算**不再使用** `rel_point_objective.weight` 加权（规约 3.1 修复，统一求和比）；
- 代码：`AchievementCalculationServiceImpl.computeObjectiveAchievement`（包级静态）。

### 二级：课程指标点达成度 E_k

```
E_k = Σ(C̄_j × w_jk) / Σ(w_jk)
```

- **C̄_j**：一级达成度的**班级平均值**（按 `objective_id` 分组求平均，即教学班级层面）；
- **w_jk**：`weight_objective_indicator.inner_weight`；
- **聚合维度**：同一教学班、同一指标点下所有课程目标；
- 因 ∑w=1.0，公式等价于 `Σ(C̄_j × w_jk)`，代码中仍除以 `Σ(w_jk)` 作安全兜底；
- 产出表：`course_indicator_achievement`（按 `teaching_class_id + indicator_id`）。

### 三级：专业指标点达成度 G_k

```
G_k = Σ(Ē_c × W_c) / Σ(W_c)
```

- **Ē_c**：课程级二级达成度的**均值**（同一课程下各教学班 `course_indicator_achievement` 求平均）；
- **W_c**：`matrix_course_indicator.total_weight`；
- **聚合维度**：同一专业 + 学年 + 年级、同一指标点下所有支撑课程；
- 产出表：`major_indicator_achievement`（按 `major_id + term_id + grade + indicator_id`）。

---

## 六、权重校验规则（∑W=1.0 / ∑w=1.0）

两套权重在保存时**强制校验**，容差均为 **0.0001**。

| 权重 | 表/字段 | 求和维度 | 校验方法 |
|------|---------|----------|----------|
| 宏观 W | `matrix_course_indicator.total_weight` | 同一**指标点**下所有支撑课程的 W 之和 = 1.0 | `MatrixCourseIndicatorServiceImpl.checkMatrixWeights` |
| 内部 w | `weight_objective_indicator.inner_weight` | 同一**课程**内、同一**指标点**下所有课程目标的 w 之和 = 1.0 | `WeightObjectiveIndicatorServiceImpl.checkWeights` |

**业务含义**：
- **W_c**：专业层面，课程 c 对指标点 k 的宏观支撑强度；
- **w_jk**：课程层面，课程目标 j 对指标点 k 的微观贡献度；
- 二者分层加权，完成「学生成绩 → 专业指标点达成度」的完整传导。

---

## 七、成绩单三态状态机

由 `manager/GradesheetStatusHelper` 推断（基于数据是否存在，非显式字段）：

```
NOT_SUBMITTED ──录入成绩──→ SUBMITTED ──课程级计算──→ LOCKED
   (无成绩)                   (有成绩、未算)            (已算出达成度)
```

- `LOCKED` 状态下再次计算需显式传 `forceRecalculate=true`，防误触覆盖；
- 锁定后仍可修改成绩，但需强制重算才能刷新达成度；
- 三级看板（`MajorCalculation.dashboard`）复用同样的 status 字段展示各教学班状态。

---

## 八、关键约束与注意事项

| 约束 | 说明 |
|------|------|
| 计算必须按级触发 | 三级依赖二级、二级依赖一级，不能跳级 |
| 专业级计算要求全部课程已算 | 任一支撑课程未完成二级计算，三级直接报错 |
| 三态锁定防重算 | LOCKED 后需 `forceRecalculate=true` 才能重算 |
| 计算结果「先删后写」 | 用事务 + 显式 `setRollbackOnly()` 保证一致性 |
| 两套权重 ∑=1.0 | W（专业层）、w（课程层），保存时强制校验，容差 0.0001 |
| 教师数据隔离 | `teacher` 仅能操作自己主讲的班/课，`admin` 放行 |
| 教学班无 major_id | 专业关联走 `course.major_id`（勿在 teaching_class 找 major） |
| leader 专业归属未收紧 | `MajorReport.validateMajorPermission` 对 leader 恒 true（`sys_user` 无 `major_id`，需 schema 变更才能收紧，已知遗留） |

---

## 九、附录：表清单与关键代码位置

### 表清单（19 张）

| 分类 | 表名 | 说明 |
|------|------|------|
| 字典 | `sys_dict_college` / `sys_dict_major` / `sys_dict_school_year` | 学院 / 专业 / 学年学期 |
| 用户 | `sys_user` | 四大角色账号 |
| 基础数据 | `course` / `student` / `teaching_class` / `class_student` | 课程 / 学生 / 教学班 / 班级-学生 |
| 毕业要求 | `graduation_requirement` / `indicator_point` | 毕业要求 / 指标点 |
| 权重 | `matrix_course_indicator`（W）/ `weight_objective_indicator`（w） | 宏观矩阵 / 内部权重 |
| 课程大纲 | `course_objective` / `assessment_point` / `rel_point_objective` | 课程目标 / 考核点 / 关联（多对多） |
| 成绩 | `student_score` | 学生考核点原始成绩 |
| 达成度结果 | `student_objective_achievement` / `course_indicator_achievement` / `major_indicator_achievement` | 一级 / 二级 / 三级 |

### 关键代码位置

| 关注点 | 位置 |
|--------|------|
| 三级达成度计算 | `service/impl/AchievementCalculationServiceImpl`（一级、二级）、`service/impl/MajorCalculationServiceImpl`（三级） |
| 一级公式（Σ得分/Σ满分） | `AchievementCalculationServiceImpl.computeObjectiveAchievement`（包级静态） |
| 专业范围过滤（major+term+grade） | `manager/MajorScopeHelper.getTeachingClasses` |
| 成绩单锁定状态推断 | `manager/GradesheetStatusHelper` |
| 数据级归属校验 | `manager/OwnershipHelper`（`checkClassOwnership`/`checkCourseOwnership`，admin 放行） |
| 鉴权 AOP | `aop/AuthInterceptor`（mustRole + anyRole）、`aop/SysJwtInterceptor`（设 currentUser）、`config/WebMvcConfig`（白名单） |
| 宏观矩阵 ∑W=1.0 校验 | `MatrixCourseIndicatorServiceImpl.checkMatrixWeights`（容差 0.0001） |
| 内部权重 ∑w=1.0 校验 | `WeightObjectiveIndicatorServiceImpl.checkWeights`（容差 0.0001） |
| 报表（D-1 / D-2） | `service/impl/CourseAchievementReportServiceImpl`、`service/impl/MajorReportServiceImpl` |
| 数据库 schema | `sql/create_table.sql` |

---

### 典型协作时序

```
admin   ──阶段0──→ 建学院 / 专业 / 学年 / 账号
edu     ──阶段1──→ 建课程、学生、教学班、绑学生
leader  ──阶段2──→ 建毕业要求、指标点
teacher ──阶段3A─→ 建课程目标、考核点、绑关联、配 w (∑w=1)
leader  ──阶段3B─→ 配宏观矩阵 W (∑W=1)
teacher ──阶段4──→ 导入成绩            [NOT_SUBMITTED → SUBMITTED]
teacher ──阶段5──→ 触发课程级计算       [SUBMITTED → LOCKED]   产出 C、E
leader  ──阶段6──→ 触发专业级计算                               产出 G
各角色  ──阶段7──→ 导出报表
```
